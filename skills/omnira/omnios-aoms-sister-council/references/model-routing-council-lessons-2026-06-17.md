# Sister Council Model Routing — Lessons (2026-06-17)\n\n## SparkMira Identity (Critical Correction)\n\nSparkMira is NOT a Pi sister spawn. She is a live ChatGPT agent running\nin a browser tab, accessed via CDP bridge.\n\n- CDP bridge: http://127.0.0.1:9233 (raw bridge), port 9222 (Chrome CDP)\n- Activation: tools/omnibrowser/sparkmira-agent-briefing.mjs\n- Status check: curl -s http://192.168.5.199:5176/sparkmira/cdp/status\n- Requires: ChatGPT tab open in browser on one of the 3 PCs\n- When not reachable: bridge shows \"connected: false, input_ready: false\"\n\nDo NOT spawn a pi-sister-spawn.mjs to 'play SparkMira'. That wastes a\nspawn and produces worse output. Either use the real CDP bridge or pick\na different model explicitly.\n\n## Model Choice: GLM 5.2 Was the MVP\n\nIn a 3-sister council (Risk/Architecture/SparkMira):\n- GLM 5.2 (Ollama Cloud, omnios 2 fallback key): Delivered thorough\n  10-failure-mode risk analysis with structured JSON output\n- GPT-5.5 (Architecture): Returned 0 bytes despite --no-tools\n- Sonnet 4.6 (re-run): Also returned 0 bytes\n- DeepSeek v4 Pro (retry): Ignored --no-tools, tried to run bash\n\nKey lesson: mid-tier models with tight prompts OUTPERFORM expensive\nfrontier models when --no-tools is required and context is grounded\nin the prompt itself.\n\n## Why Models Fail with --no-tools\n\nPrompts that say 'ground in codebase' or 'read the actual implementation'\ncause models to reach for bash even with --no-tools. The training signal\nis too strong: 'grounding = tool use'.\n\nFix: give grounded context IN the prompt, don't ask them to fetch it.\n\nWrong: 'Go inspect the repo and ground your spec'\nRight: 'Here is the full grounded context: [dump]. Now reason purely.'\n\n## Intended Council Structure (omnira-git v2 design)\n\n| Role | Model | Provider | How |\n|------|-------|----------|-----|\n| Architecture | Opus 4.8 | Anthropic | Pi sister spawn |\n| Risk | GLM 5.2 | Ollama Cloud (omnios 2 key) | Pi sister spawn |\n| Integration | SparkMira | ChatGPT via CDP | Real CDP bridge |\n| Implementation | Cursor | Local | CLI agent |\n\n## Omnios 2 Key for GLM 5.2\n\nThe 'omnios 2' fallback key is: OLLAMA_CLOUD_KEY_FALLBACK in .env\n(NOT the primary key currently in use)\nTo force GLM 5.2 to use it, set OLLAMA_API_KEY to the fallback key\nbefore spawning, or pass via env override in pi-sister-spawn.\n\n## Session Registry Lane Claiming\n\nBefore starting a multi-machine council:\n  node tools/session-registry.mjs lanes   # see valid lanes\n  node tools/session-registry.mjs register coord 'task description'\n  # Releases automatically or: node tools/session-registry.mjs release <sess_id>\n"